[project]
name = "ollama-client"
version = "0.1.0"
description = "Python client library for Ollama server"
readme = "README.md"
requires-python = ">=3.7"
dependencies = [
    "requests>=2.31.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

